> 本文档是参与本项目的**必读指导文档**。
> 参与本项目前，必须首先完整阅读本文档。

---

# HomeReel 项目介绍

## 项目定位

HomeReel 的目的是 **局域网视频资料管理与播放平台**，经 Web 供 PC / 手机 / 平板 / TV 等终端访问。

功能描述：
+ 基础文件管理，包括 浏览 / 上传 / 下载 / 重命名 / 移动 / 删除 等
+ 维护视频元数据（剧集分组等），并提供各种格式的视频播放功能
+ 全局单一口令登入，不区分用户，但 **支持多终端并发访问**，会话独立不干涉
+ 后端部署在 Windows 平台，前端具备跨平台能力（响应式UX）

## 技术栈

**前端**：React 19 + TypeScript、Vite、Tailwind CSS 4、TanStack Router / Query、Vidstack、oxlint
**后端**：Go（1.26）、`net/http`（1.22+）、`slog`、SQLite（`modernc.org/sqlite`，WAL）
**媒体**：FFmpeg / ffprobe
**前端包管理**：`pnpm`（禁止 npm / cnpm / yarn）

## 核心原则

> 开发全程都必须考虑的原则性内容

**元数据与文件存储分离**
+ 元数据仅由数据库维护，文件存储仅由文件系统承载，互不干涉
+ 组织关联等功能全部基于元数据，文件系统中的原多媒体文件不受影响

**功能正确优先，禁止过度优化**
+ 未见真实性能瓶颈、缺陷时，禁止预先设计复杂的并发优化、抽象架构等

**AI 友好与长期维护**
+ 模块化、低耦合、显式优先、避免过度抽象的设计模式
+ 保持逻辑流畅，可阅读性强

# 文件组织

## 文档体系

> 开发前按需阅读；`docs/decisions.md`（骨架决策 + 契约）优先级最高。

| 文档 | 定位 | 阅读时机 |
|---|---|---|
| `docs/decisions.md` | 架构决策 / 数据模型 / API 契约 / 配置 / 演进路线 | 评估架构影响、查表结构、API、config |
| `docs/environment.md` | 开发环境、FFmpeg PATH、部署 | 搭建环境、排障 |
| `docs/backend.md` | 后端实现事实源 | 改动 `backend/internal/` |
| `docs/media.md` | 媒体管线实现事实源 | 改动 `media/streaming` |
| `docs/frontend.md` | 前端实现事实源 | 改动 `frontend/src/` |
| `docs/realtime.md` | WebSocket 实时通道 | 改动 `realtime/` |
| `docs/status.md` | 现状快照 / 遗留待办 | 规划新功能 |

**规则**：文档与代码不一致时，以 decisions.md 契约为准，并向用户指出差异。

## 工程目录

```
backend/
  cmd/server            # 入口
  internal/             # 核心业务包 | 内部按模块划分
  data/                 # 运行数据：SQLite + 缓存文件
  config.yaml           # 服务配置

frontend/
  src/
    api/       # API 调用
    components/# 通用组件
    features/  # 功能模块 auth/files/home/library/...
    lib/       # 工具库
    tabs/      # 页面路由与页签宿主
```

# 工作规范

## 沟通规范

**回复风格**
- 直接、精确、简洁，避免冗长的铺垫
- 以中文回复，无论输入是何种语言
- 引用任何文件内容时，使用 `文件:行号` 格式

**积极提问**
- 当用户表述模糊、存在歧义时，向用户询问以确认真实需求
- 当存在多种可行方案时，给出 2~3 个候选方案，说明各自利弊，标出推荐项
- 无显著区别的选项不必询问，如两个依赖包几乎可互相等价替代，由你自己评估决定

## 开发规范

**维持文档与项目的一致性**
- 新增关键知识、全局要求等，立即更新进对应文档
- 必读的核心内容写入本文档
- 只影响特定领域的写入 `docs/` 下的对应二级文档
- 遵循本文档的凝练简洁风格
- 开发时发现已存在的死代码、冗余注释等，将其移除或改写

**代码撰写要求**
- 优先基于变量名、函数名等代码自注释
- 仅在没有该注释会导致代码无法理解时，最小限度地加入必要注释
- 代码文件中，除非是UI文字可以使用中文，否则必须使用ASCII字符，禁止使用emoji

**严禁修补式代码**
沿原逻辑链，针对新增情况堆叠大量 `if` 特判分支，属于修补式代码。
- 当需求扩展时，应整体考虑，是否存在策略类、接口提取、面向对象等常规方法
- 常规方法无法解决，则考虑是否应该重构整体实现，依然**严禁**在调用链上打补丁

## 高危操作必先批准

以下操作，属于高危操作：
- 安装、卸载、升级任何软件依赖包或系统组件
- 修改全局环境变量、PATH、注册表、防火墙等系统级配置
- 操作项目目录之外的文件
- 删除任何文件
- 启动/停止系统服务等具有全局影响的系统组件
- 通过网络接口等工具操作外部服务，如在 Github 上创建 PR，关闭 Issue
- 向远程 Git 仓库执行非只读操作
- 部署、执行可能具有上述任意影响的程序，间接导致上述结果

若某操作被判定为高危操作，必须预先执行以下流程：
1. 向用户说明操作目的，操作影响
2. 使用 Question 工具向用户询问是否批准

## 红线操作禁止执行

以下操作，属于红线操作：
- 执行永久阻塞命令，例如：
  - 使用命令行启动一个后台 server，而该 server 除非手动 Ctrl-C 不会自动关闭
  - 在合理时间内无法执行完毕的命令

## 人工辅助策略

对于某个在开发阶段要进行的操作，若属于以下范畴：
- 无法使用命令行执行
- 使用命令行的难度、效果远不如人工执行
- 判断UI显示效果、UX交互便利度等主观感受

不强行使用脚本、外部开源库等方式模拟，而是交由用户手工辅助：
1. 明确告知用户需手动执行的内容，以及执行方法
2. 声明用户需要反馈何种信息

## 开发验证

需求开发完成后，必须执行验证操作，清单如下

**后端**
- 单元测试：`go test ./...`
- 静态分析：`go vet ./...`
- 构建验证：`go build ./cmd/server`

**前端**
- 代码检查：`pnpm run lint`
- 构建验证：`pnpm run build`

## 收尾

**主动清理临时垃圾**
以下内容属于临时垃圾，应当在操作结束后清除
- 执行某项任务过程中，产生的中间输出文件
- 撰写的一次性使用脚本、程序
- 残留的进程，例如以非阻塞

# 特殊用户指令

## Git Operations

当用户提出 提交本次会话的修改 时，使用 Git 工具提交到远程仓库

```bash
git add {SOME FILES}
git commit -m "{COMMIT MESSAGE}"
git push
```

The Commit Message should be a short, clear, accurate and concise English phrase describing the modification made in this session.
Provide 3 message options (with one of them marked as recommended), then call Question-Tool to ask the user which one is appropriate.